# Dependencies live in pyproject.toml. This file is kept so that the habitual
#     pip install -r requirements.txt
# still does the right thing.
#
# On a Raspberry Pi you want the hardware extra instead:
#     pip install -e '.[pi]'
#
# Needs a reasonably recent pip. Versions before ~21.3 cannot install an
# editable pyproject.toml project and will skip the line with only a warning,
# leaving nothing installed -- run `pip install --upgrade pip` first.
-e .
